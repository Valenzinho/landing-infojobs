---
import SearchIcon from "src/icons/Search.astro";

const SHORTCUTS = [
  {
    label: "🌱  Sin experiencia",
    href: "https://www.infojobs.net/jobsearch/search-results/list.xhtml?keyword=&segmentId=&page=1&sortBy=PUBLICATION_DATE&onlyForeignCountry=false&countryIds=17&sinceDate=ANY&experienceMin=_0_YEARS&experienceMax=_0_YEARS",
  },
  {
    label: "🗓️  Findes",
    href: "https://www.infojobs.net/ofertas-trabajo/fines-de-semana",
  },
  {
    label: "🕒  Media jornada",
    href: "https://www.infojobs.net/ofertas-trabajo/media-jornada",
  },
  {
    label: "💼  Prácticas",
    href: "https://www.infojobs.net/ofertas-trabajo/practicas",
  },
] as const;
---

<section class="mx-auto max-w-7xl px-4 w-full">
    <div class="bg-hero-pattern mx-auto w-full rounded-3xl px-4 py-6">
        <h1 class="text-black/80 font-semibold tracking.[0.35px] text-2xl text-center pb-4">
            Tu carrera empieza aqui
        </h1>
        
        <search class="bg-white rounded-3xl p-4">
            <form>
                <div>
                    <select class="w-full py-4 border-b border-gray-300 text-gray-600">
                        <option value="" disabled selected>Nivel de estudios</option>
                        <option value="1">Bachillerato</option>
                        <option value="2">Grado</option>
                        <option value="3">Master</option>
                    </select>
                </div>

                <div>
                    <input
                        placeholder="Estudios, puesto, empresa..."
                        class="w-full py-4 border-b border-gray-300 text-gray-600"
                    />
                </div>

                <div class="mb-8">
                    <input 
                        placeholder="Provincia o poblacion"
                        class="w-full py-4 text-gray-600"
                    />
                </div>
                <button class="rounded-4xl text-lg font-medium bg-accent hover:saturate-150 transition text-white px-4 py-2 inline-flex gap-x-2 justify-center items-center">
                    <SearchIcon />
                    Buscar ofertas
                </button>
                <footer>
                    <p class="text-center pt-8 pb-4">Explora trabajos que se adaptan a ti</p>
                    <div
                      class="max-xs:grid-cols-1 max-sm:grid max-sm:grid-cols-2 flex flex-wrap gap-4 justify-center"
                    >
                      {
                        SHORTCUTS.map(({ label, href }) => (
                          <Button
                            as="link"
                            href={href}
                            color="default"
                            variant="solid"
                            radius="full"
                            size="lg"
                            class="bg-white whitespace-nowrap text-center rounded-full p-4 lg:px-6 shadow-sm text-primary transition hover:scale-105 hover:text-white hover:bg-primary !font-light"
                            disableSaturateHover={true}
                          >
                            {label}
                          </Button>
                        ))
                      }
                    </div>
                </footer>
            </form>
        </search>
    </div>
</section>